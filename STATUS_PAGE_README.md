# صفحة حالة الخدمات - Status Page

## نظرة عامة
صفحة حالة خدمات احترافية تعرض حالة جميع خدمات منصة مدعوم بشكل مشابه لـ Metastatus.com

## الميزات الرئيسية

### 1. **عرض حالة الخدمات**
- عرض جميع خدمات المنصة مع حالتها الحالية (تعمل بشكل طبيعي / أداء منخفض / معطلة)
- بطاقات تفاعلية لكل خدمة مع معلومات تفصيلية
- رموز بصرية واضحة لسهولة الفهم

### 2. **إحصائيات ملخصة**
- عدد الخدمات التي تعمل بشكل طبيعي
- عدد الخدمات بأداء منخفض
- عدد الخدمات المعطلة
- متوسط التوفر الشهري (Uptime)

### 3. **رسوم بيانية للتوفر**
- عرض توفر كل خدمة على مدار آخر 30 يوم
- رسوم بيانية ملونة توضح فترات التعطل والأداء المنخفض
- معلومات تفصيلية عند تمرير المؤشر على كل يوم

### 4. **نظام الفلاتر**
- تصفية الخدمات حسب الحالة (جميع الخدمات / تعمل بشكل طبيعي / أداء منخفض / معطلة)
- بحث سريع وتصفية فورية

### 5. **قسم الحوادث والصيانة**
- عرض جميع الحوادث الحالية والسابقة
- معلومات عن الخدمات المتأثرة
- حالة الحادثة (تحت التحقيق / تم تحديده / قيد المراقبة / تم الحل)
- التواريخ والأوقات الدقيقة

### 6. **تحديثات فورية**
- تحديث آخر وقت تحديث تلقائياً
- اتصال فوري بـ Supabase للحصول على البيانات الحية
- إشعارات بالحوادث الجديدة

### 7. **تصميم احترافي**
- واجهة مستخدم حديثة وسهلة الاستخدام
- دعم كامل للعربية (RTL)
- متجاوب مع جميع أحجام الشاشات (Mobile, Tablet, Desktop)
- ألوان احترافية وتصميم جذاب

## الملفات المضافة

### 1. `admin/status-page.html`
الصفحة الرئيسية لعرض حالة الخدمات
- تصميم HTML/CSS/JavaScript كامل
- بيانات وهمية للاختبار (يمكن استبدالها بـ API حقيقي)
- فلاتر وتصفية تفاعلية

### 2. `service-status.js`
مكتبة JavaScript لإدارة بيانات الخدمات
- دوال للاتصال بـ Supabase
- جلب بيانات الخدمات والحوادث
- تحديث حالة الخدمات
- الاشتراك في التحديثات الفورية

### 3. `migrations/001_create_status_tables.sql`
ملف SQL لإنشاء جداول قاعدة البيانات
- جدول `services` - لتخزين معلومات الخدمات
- جدول `service_status_history` - لتسجيل تاريخ حالة الخدمات
- جدول `incidents` - لتسجيل الحوادث والصيانة
- Indexes و RLS policies

## كيفية الاستخدام

### 1. إعداد قاعدة البيانات
قم بتشغيل ملف SQL في Supabase:
```bash
# انسخ محتوى ملف migrations/001_create_status_tables.sql
# وقم بتشغيله في Supabase SQL Editor
```

### 2. الوصول إلى الصفحة
```
https://mad3oom.online/admin/status-page.html
```

### 3. تحديث بيانات الخدمات
استخدم الدوال المتاحة في `service-status.js`:

```javascript
import { serviceStatusManager } from '/service-status.js';

// جلب جميع الخدمات
const services = await serviceStatusManager.fetchServices();

// تحديث حالة خدمة
await serviceStatusManager.updateServiceStatus('service-id', 'operational', 45);

// إنشاء حادثة جديدة
await serviceStatusManager.createIncident(
    'عنوان الحادثة',
    'وصف الحادثة',
    ['خدمة 1', 'خدمة 2']
);
```

## البيانات الوهمية

الصفحة تأتي مع بيانات وهمية للاختبار:
- 8 خدمات مختلفة
- حالات متنوعة (operational, degraded)
- حوادث عينة

يمكنك استبدال هذه البيانات بـ API حقيقي بسهولة.

## التخصيص

### تغيير الألوان
عدّل متغيرات CSS في `:root`:
```css
:root {
    --color-success: #10b981;
    --color-warning: #f59e0b;
    --color-danger: #ef4444;
    /* ... */
}
```

### إضافة خدمات جديدة
أضف سجلات جديدة في جدول `services` في Supabase

### تخصيص الحوادث
عدّل قسم الحوادث في الصفحة أو استخدم API لإدارتها

## التكامل مع الأنظمة الأخرى

### Monitoring Systems
يمكن ربط الصفحة مع أنظمة المراقبة مثل:
- Prometheus
- Grafana
- New Relic
- DataDog

### Alerting
يمكن إضافة نظام تنبيهات:
- إرسال بريد إلكتروني عند تعطل خدمة
- إرسال رسائل Slack/Discord
- إشعارات الويب (Web Notifications)

## الأمان

- جميع البيانات محمية بـ RLS (Row Level Security)
- الوصول العام للقراءة فقط
- تحديثات الحالة محمية بـ Authentication

## الأداء

- تحميل سريع للصفحة
- استعلامات محسّنة مع Indexes
- تحديثات فورية بدون تحديث الصفحة
- تخزين مؤقت للبيانات

## الدعم والصيانة

للمزيد من المعلومات أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.

---

**آخر تحديث:** يونيو 2026
**الإصدار:** 1.0
