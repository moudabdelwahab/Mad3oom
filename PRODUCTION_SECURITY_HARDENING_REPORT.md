# تقرير تقوية الأمان على الإنتاج

**المشروع:** Mad3oom · **قاعدة الإنتاج:** `srnelrdpqkcntbgudyto`
**الفرع:** `claude/brave-clarke-or4axu` · **PR:** [#76](https://github.com/moudabdelwahab/Mad3oom/pull/76)
**التاريخ:** 2026-09-18
**المرجع:** `FULL_PROJECT_AUDIT.md` (7 حرجة · 10 عالية · 19 متوسطة · 13 منخفضة)

> **قاعدة هذا التقرير:** لا يُعتبر إصلاحٌ ناجحًا لأن `apply_migration` ردّ
> `success`. كل بند أدناه أُعيد استغلاله فعليًا على قاعدة الإنتاج بعد الإصلاح،
> وكل اختبار جرى داخل `transaction` يُتراجَع عنه بـ`rollback`. وحيث تعذّر
> التحقق، قيل ذلك صراحةً في §6 بدل أن يُترك ضمنيًا.

---

## 1. ملخّص تنفيذي

| | العدد |
|---|---|
| ثغرات أُغلقت وأُثبت إغلاقها | **14** (7 حرجة + 7 عالية) |
| ترحيلات جديدة طُبِّقت على الإنتاج | 5 (043، 044، 045، 046، 047) |
| ترحيلات قائمة طُبِّقت | 023 (جزئيًا 027) |
| دوال حافة أُعيد نشرها | 5 |
| اختبارات استغلال أُعيد تشغيلها | 34 — **كلها بالنتيجة المتوقَّعة** |
| بيانات إنتاج تغيّرت | **صفر** (كل الاختبارات تراجعت) |
| إصلاحات أُوقفت عمدًا وموثَّقة | 2 (027 §1 و§2b) |

**ثلاثة اكتشافات لم تكن في التدقيق الأصلي، ظهرت أثناء الإصلاح:**

1. **عُطل قائم في الإنتاج:** الواجهة كانت تنادي
   `request_subscription_purchase` و`cancel_my_subscription_request` وهما **غير
   موجودتين** في القاعدة. أي أن **شراء الاشتراك كان معطوبًا فعلًا**، وتطبيق 023
   أصلح عطلًا قائمًا لا خاطر بعطل جديد.
2. **عُطل قائم ثانٍ:** مسار التحقق من رمز تيليجرام لم ينجح **ولا مرة واحدة**
   منذ إنشائه — الواجهة ترسل `{userId, code}` والدالة تقرأ `{userId, otp}`.
   و`telegram_auth_logs` الفارغ تمامًا (0 صفًا) يؤكّد ذلك.
3. **ثغرة جديدة:** `admin_telegram_otps` كانت تحمل سياسة قراءة
   `auth.uid() = user_id` — أي أن صاحب الحساب يقرأ `otp_hash` الخاص به، وهو
   SHA-256 لستة أرقام يُكسر محليًا في أجزاء من الثانية. العامل الثاني كان يسقط
   أمام أي مهاجم يملك جلسة.

---

## 2. جدول قبل → بعد (إعادة تشغيل كل اختبارات الاستغلال)

كل صفّ نُفِّذ على الإنتاج داخل `begin … rollback`.

### 2.1 الثغرات الحرجة

| # | الاستغلال | قبل | بعد |
|---|---|---|---|
| C-01 | عميل يمنح نفسه اشتراكًا فعّالًا بـINSERT مباشر | ✅ ينجح | **DENIED 42501** ✓ |
| C-01 | المسار الشرعي `request_subscription_purchase('whatsapp','monthly')` | ❌ الدالة غير موجودة | **OK — status=pending** ✓ |
| C-02 | عميل يرقّي نفسه إلى `admin` | ✅ ينجح | **DENIED 42501** ✓ |
| C-02 | عميل يضبط `is_verified` لنفسه | ✅ ينجح | **DENIED 42501** ✓ |
| C-02 | محظور يفكّ الحظر عن نفسه | ✅ ينجح | **DENIED 42501** ✓ |
| C-02 | المسار الشرعي: تعديل الاسم | ✅ | **OK** ✓ |
| C-03 | صاحب مفتاح API يرفع `scopes` إلى `admin:full` | ✅ ينجح | **DENIED 42501** ✓ |
| C-03 | صاحب المفتاح يمسح `expires_at` | ✅ ينجح | **DENIED 42501** ✓ |
| C-03 | صاحب المفتاح يعيد إسناد `user_id` | ✅ ينجح | **DENIED 42501** ✓ |
| C-03 | المسار الشرعي: تعطيل المفتاح | ✅ | **OK** ✓ |
| C-04 | حاويتا `tickets` و`chat-attachments` عامّتان | ✅ عامّتان | **`public=false` للاثنتين** ✓ |
| C-05 | `anon` يرفع ملفًا إلى `ticket-attachments` | ✅ ينجح | **DENIED 42501** ✓ |
| C-05 | سياسات رفع بلا حصر بالمالك | 1 سياسة مفتوحة | **0 — كل السياسات الأربع محصورة** ✓ |
| C-06 | `anon` يبلّغ عن خطأ بحالة `resolved` | ✅ ينجح | **DENIED 42501** ✓ |
| C-06 | `anon` يحشو رسالة 5000 حرف | ✅ ينجح | **DENIED 42501** ✓ |
| C-06 | المسار الشرعي: بلاغ خطأ عادي | ✅ | **OK** ✓ |
| C-07 | `gemini-proxy` يقرأ بيانات مستخدم آخر عبر `userId` من الجسم | ✅ ينجح | **v47 = 410 بلا أي وصول للقاعدة** ✓ |

### 2.2 الثغرات العالية

| # | الاستغلال | قبل | بعد |
|---|---|---|---|
| H-02 | صاحب الحساب يقرأ `otp_hash` ويكسره محليًا | ✅ يقرأه | **قرأ 0 صفًا** ✓ |
| H-02 | مستخدم عادي ينادي بوابة الخنق مباشرةً | — (لم تكن موجودة) | **DENIED 42501** ✓ |
| H-02 | البوابة تسمح لمستخدم بلا محاولات فاشلة | — | **ALLOWED** ✓ |
| H-02 | البوابة بعد 5 محاولات فاشلة | ✅ لا خنق إطلاقًا | **BLOCKED: user_attempts_exceeded** ✓ |
| H-02 | البوابة بعد 20 فشلًا من نفس الـIP خلال 15 دقيقة | ✅ لا حدّ | **BLOCKED: ip_attempts_exceeded** ✓ |
| H-02 | الحظر لا يتسرّب إلى عنوان IP نظيف | — | **ALLOWED** ✓ |
| H-02 | تسجيل إجراء غير معروف في سجلّ المصادقة | — | **REJECTED 22023** ✓ |
| H-03 | فحص الرصيد قبل الإرسال (مبلغ ضمن الرصيد) | ❌ لا فحص إطلاقًا | **ALLOWED** ✓ |
| H-03 | فحص الرصيد قبل الإرسال (مبلغ يتجاوز الرصيد) | ❌ لا فحص | **DENIED** ✓ |
| H-03 | خصم رسالة مُرسَلة | ❌ لا خصم | **48.04 → 47.76** ✓ |
| H-03 | خصم أكبر من الرصيد | ❌ | **DENIED** ✓ |
| H-03 | كشف التكرار بنفس معرّف الرسالة | ❌ | **سجل واحد فقط** ✓ |
| H-04 | حساب محظور ما زال يقرأ تذاكره وإشعاراته | ✅ تذاكر=2 إشعارات=22 | **تذاكر=0 إشعارات=0** ✓ |
| H-04 | محظور ينشئ تذكرة جديدة | ✅ ينجح | **DENIED 42501** ✓ |
| H-06 | `meta-webhook` يقبل أي طلب حين يغيب `META_APP_SECRET` | ✅ fail-open | **v21 fail-closed + مقارنة ثابتة الزمن** ✓ |
| H-07 | `huggingface-chatbot` يقرأ مفتاح المزوّد | ✅ | **v36 = 410، `verify_jwt=true`** ✓ |
| H-05 | إدراج موضوع منتدى باسم حساب الإدارة | ✅ ينجح | **DENIED 42501** ✓ |
| H-05 | إدراج بلاغ منتدى باسم حساب الإدارة | ✅ ينجح | **DENIED 42501** ✓ |
| H-05 | إدراج منشور مجتمع باسم حساب الإدارة | ✅ ينجح | **DENIED 42501** ✓ |
| H-05 | إدراج تعليق مجتمع باسم حساب الإدارة | ✅ ينجح | **DENIED 42501** ✓ |
| H-05 | المسار الشرعي: إدراج باسم النفس | ✅ | **OK** ✓ |
| H-06 | `meta-webhook` يقبل أي طلب حين يغيب `META_APP_SECRET` | ✅ fail-open | **v21 fail-closed + مقارنة ثابتة الزمن** ✓ |
| H-07 | `huggingface-chatbot` يقرأ مفتاح المزوّد | ✅ | **v36 = 410، `verify_jwt=true`** ✓ |
| H-08 | سرّ Turnstile داخل ملف يُنزّله المتصفح | ✅ مكشوف | **أُزيل من الملف ومن التقرير** ⚠️ يجب تدويره |
| H-10 | مستخدم يكتب بريد الإدارة في ملفه لاختطاف السلطة | ✅ ينجح | **DENIED 42501** ✓ |

**مجموع الاختبارات: 34 — كلها بالنتيجة المتوقَّعة.**

> **عن H-05 تحديدًا — تصحيح لقراءة أولى خاطئة:** أول اختبار لهذه الثغرة ردّ
> `23502` فبدا كأنه منع. لم يكن: `forum_threads.slug` عمود NOT NULL بلا قيمة
> افتراضية، وكان غيابه هو سبب الرفض لا السياسة. أُعيد الاختبار بكل الأعمدة
> الإلزامية فجاء **ALLOWED** — أي أن الثغرة كانت **ما زالت مفتوحة**. ولولا
> إعادة الاختبار لسُجِّلت «مغلقة» زورًا. ثم طُبِّق الترحيل 047 وأُعيد الاستغلال
> فجاء `DENIED 42501` على المسارات الأربعة.

> **عن H-10:** خطرها الأمني — كما وصفه التدقيق نفسه — كان مشروطًا بكون
> `profiles.email` قابلًا للكتابة من العميل. وحارس C-02 يحمي `email` ضمن
> أعمدته، فمسار الاختطاف مغلق الآن ومُثبَت. **لكن التصميم نفسه باقٍ:** ما زالت
> سياسات ودوال عدة تشتق السلطة من عنوان بريد ثابت نصًّا. ذلك دَين تصميمي
> يُعالَج بترحيل لاحق، لا ثغرة مفتوحة اليوم.

---

## 3. ما طُبِّق على الإنتاج بالضبط

### 3.1 ترحيلات جديدة

| الترحيل | الغرض | الحالة |
|---|---|---|
| `043_api_token_mutation_guard.sql` | محفّز BEFORE UPDATE يمنع تعديل `scopes`، `user_id`، `expires_at`، `api_key`، `secret_hash`، وأخواتها. يبقى `is_active` و`revoked_at` قابلَين للكتابة لأنهما إجراءا الواجهة الشرعيان. | ✅ مُطبَّق ومُتحقَّق |
| `044_site_errors_status_integrity.sql` | قيد CHECK على `status`، وسياسة INSERT عامّة تحدّ الأطوال وتحصر `type` وتثبّت `status='new'`. | ✅ مُطبَّق ومُتحقَّق |
| `045_whatsapp_send_billing_settings.sql` | `wallet_enforce_send=false` و`text_charge_egp` في `integration_settings`. | ✅ مُطبَّق ومُتحقَّق |
| `046_otp_verification_hardening.sql` | إسقاط سياسة قراءة `admin_telegram_otps`، ونقل قرار الخنق إلى `otp_attempt_gate`، و`otp_log_attempt`، وثلاثة فهارس، وسحب `increment_otp_attempts` من anon/authenticated. | ✅ مُطبَّق ومُتحقَّق |
| `047_forum_author_identity.sql` | ربط عمود الكاتب بـ`auth.uid()` في سياسات الإنشاء الخمس (المنتدى والمجتمع والبلاغات). الشرط كان «أن تكون مسجّلًا» فصار «أن تكون أنت». | ✅ مُطبَّق ومُتحقَّق |

### 3.2 ترحيلات قائمة في المستودع

| الترحيل | القرار |
|---|---|
| `023` | ✅ طُبِّق بالكامل. **أصلح عطلًا قائمًا**: الواجهة كانت تنادي دالتين غير موجودتين. |
| `027` §2a | ✅ طُبِّق (حارس UPDATE على `profiles`). |
| `027` §5 | ✅ طُبِّق. |
| `027` §1 | ⛔ **أُوقف** — تفصيل في §5. |
| `027` §2b | ⛔ **أُوقف** — تفصيل في §5. |
| `028`، `030` | ✅ طُبِّقا (خصخصة التخزين). |
| `012` | ⛔ **لا يُطبَّق** — تفصيل في §5. |

### 3.3 دوال الحافة

| الدالة | الإصدار | التغيير | `verify_jwt` |
|---|---|---|---|
| `gemini-proxy` | v47 | كعب 410 بلا أي وصول للقاعدة (C-07) | true |
| `huggingface-chatbot` | v36 | كعب 410، لا يقرأ مفتاح المزوّد (H-07) | **false → true** |
| `meta-webhook` | v21 | التحقق من التوقيع يفشل **مغلقًا** + مقارنة ثابتة الزمن (H-06) | false (webhook) |
| `send-whatsapp` | v31 | فحص رصيد قبل الإرسال + خصم بعد قبول Meta (H-03) | false |
| `mcp` | v33 | نفس المنطق عبر وحدة مشتركة (H-03) | false |
| `verify-otp` | v34 | الهوية من التوكن، والخنق قبل المطابقة، وحدّ IP (H-02) | **false → true** |

### 3.4 الواجهة

- `admin/errors.html` — حصر `status` و`type` في قوائم بيضاء قبل وصولهما إلى
  فئة CSS، وتحويل العدّادات إلى أرقام. يغلق مسار XSS المخزَّن الذي فتحه C-06.
- `turnstile-config.js` — أُزيل `SECRET_KEY` من الملف الذي يُنزّله المتصفح.
- `telegram-otp.html` — يرسل `otp` بدل `code`، ولم يعد يرسل `userId`، ويُظهر
  رسالة الخادم عند 429 بدل «رمز غير صحيح» المُضلِّلة.

### 3.5 CI

- `scripts/drift-check.mjs` + `.github/workflows/drift.yml` + `drift-baseline.json`
  — كشف الانحراف بين المستودع والإنتاج، يفشل على الانحراف **الجديد** وحده.
- `tests/drift-check.test.mjs` — 9 اختبارات لمنطق المقارنة.

---

## 4. حالة CI

| الفحص | النتيجة على `e4207ba` |
|---|---|
| node + SQL | ✅ نجح |
| فحوص المستودع (Drift) | ✅ نجح |
| مقارنة بالإنتاج (Drift) | ✅ نجح |
| CodeQL | ✅ نجح |
| Analyze (javascript-typescript) | ✅ نجح |
| Vercel Preview | ✅ نجح |

`mergeable_state: clean` — لا تعارض دمج.

**فشلان سابقان وسببهما:** الدفعتان `aa17ead` و`1d05fc9` أسقطتا `node + SQL`
بسبب فخَّين متعمَّدين في حزمة الاختبارات، وكلاهما عمل كما صُمِّم:

1. **«لا ترحيل جديد بلا قرار صريح»** — فخّ يمنع تسلّل ترحيلات. أُصلح بتوثيق
   سبب كل ترحيل من 043 إلى 046 في نفس القائمة ونقل التأكيد إلى 046.
2. **«لا شيء في المستودع يشير إلى gemini-proxy»** — أطلقه المصدر المؤرشف تحت
   `_retired/` ووثائق التدقيق وخط أساس الانحراف، ولا شيء منها *مُنادٍ*. ضُيِّق
   الاختبار ليفحص المنادين — وهو ما كانت رسالة فشله تدّعي فحصه أصلًا — مع
   تجاهل التطابق داخل التعليقات. **وأُثبت أنه لم يضعف:** زُرع منادٍ حقيقي
   فسقط الاختبار، وعاد ناجحًا بعد إزالته.

---

## 5. ما أُوقف عمدًا — ولماذا

> هذه أهم فقرة في التقرير. كل بند هنا إصلاحٌ **لم يُنفَّذ**، ولو نُفِّذ لكسر
> الإنتاج أو تراجع بالأمان.

### 5.1 `027` §2b — حارس INSERT على `profiles`

**السبب: يكسر تسجيل أي مستخدم جديد.** الترحيلة تفترض أن
`profiles.ban_status` يبدأ `NULL`، بينما قيمته الافتراضية على الإنتاج
`'none'`. فالحارس يرفض الإدراج المشروع نفسه.

**مُثبَت باختبار داخل transaction:**
`legit self-insert (ban_status = DEFAULT 'none') → BROKEN (42501)`

### 5.2 `027` §1 — إعادة تعريف دوال التفويض

**السبب: تراجع أمني صريح.** المقطع يستعمل `create or replace` على
`is_admin` و`is_support_user` و`is_platform_staff`، فيدهس نسخ الترحيلة **040**
الأحدث. والأسوأ أن `is_admin_user` فيه **يعيد قائمة البريد المسموح بها** التي
نزعتها 040 عمدًا — وعمود `email` قابل للكتابة من العميل.

طُبِّق §2a و§5 وحدهما.

### 5.3 الترحيل `012` — إعادة فتح التذكرة تلقائيًا بردّ المالك

**السبب: الترحيل 034 ألغاه عمدًا، وحزمة الاختبارات تحرس ذلك.**
`tests/sql/ticket-reopen-close.test.sql` تؤكّد صراحةً:

- `PASS 0B: قبل الإصلاح — الردّ يعيد الفتح تلقائيًا` (السلوك القديم)
- `PASS 2: الردّ حُفِظ والتذكرة بقيت مغلقة` (السلوك المطلوب)

تطبيق 012 يعيد سلوكًا أُزيل بقرار منتج ويُسقط الحزمة. **لا يُطبَّق.**

### 5.4 فرض الحظر المالي في واتساب (H-03)

**السبب: قرار صريح من صاحب المنتج — «قياس أولًا ثم حظر».** محفظة واحدة من
ثلاث ممولة (48.04 / 0.00 / 0.00)، والفرض الفوري كان سيقطع الإرسال عن
مستأجرَين. القياس والخصم يعملان الآن، و`wallet_enforce_send=false` حتى يقرّر
المالك.

### 5.5 H-01 — بنية التحقق الثنائي

**السبب: تغيير معماري، والتعليمات كانت صريحة بعدم تنفيذه.** الخطة كاملة في
`AUTH_HARDENING_PLAN.md` مع ثلاثة خيارات وتوصية وستة مخاطر وخطة ترحيل من ست
مراحل. القرار لمالك المشروع.

---

## 6. ما لم يُتحقَّق منه — بصراحة

| البند | السبب |
|---|---|
| **سلوك دوال الحافة فوق HTTP** | منفذ الشبكة إلى `*.supabase.co` محجوب من بيئة التنفيذ (`curl` يعيد HTTP 000). التحقق تمّ على **مصدر النشر** (مطابقة بايت-ببايت بين المستودع والإنتاج) وعلى **طبقة القاعدة** التي تستهلكها الدوال. وهذا بالضبط سبب نقل منطق الخنق في H-02 إلى القاعدة: ليصير قابلًا للاختبار بـSQL. |
| **خصخصة التخزين على مستوى HTTP** | لنفس السبب. التحقق تمّ على `storage.buckets.public = false` وعلى سياسات `storage.objects`. |
| **حدّ الـIP تحت حِمل حقيقي** | `x-forwarded-for` يضبطه وسيط Supabase لكنه يظل قيمة من الشبكة. الحدّ «أفضل جهد» يرفع كلفة التخمين الموزّع؛ **حدّ المستخدم هو الحدّ المُلزِم**. |
| **سجلّ الترحيلات** | `supabase_migrations.schema_migrations` غير موثوق في هذا المشروع (023–032 غير مسجَّلة رغم تطبيقها جزئيًا). فحص الانحراف يُعلن ذلك بدل أن يُفسَّر صمته نجاحًا. |
| **8 اختبارات node محليًا** | تفشل لغياب `playwright` و`xlsx` في هذه الحاوية — بيئة لا منطق. CI يثبّتهما، و**CI أخضر**. |

**تنبيه على نطاق حارسَي C-02 و C-03:** كلاهما يستثني `is_admin()` عمدًا.
وتبيّن أثناء الاختبار أن `platform_owner` يجتاز `is_admin()`. أول محاولة
لاختبار C-03 وقعت على المفتاح الوحيد في القاعدة — ويملكه `platform_owner` —
فظهرت النتيجة `ALLOWED`. **لم تكن ثغرة بل الاستثناء المقصود**، وأُعيد الاختبار
على مفتاح يملكه مستخدم عادي فجاء `DENIED 42501`. يُذكَر هنا لأن قارئ التقرير
يجب أن يعرف أن هذين الحارسين **لا يحميان من حساب إداري مُخترَق**.

---

## 7. ما لم يُمَس

- لا صفّ بيانات إنتاج واحد تغيّر. أرصدة المحافظ بعد كل الاختبارات:
  **48.04 / 0.00 / 0.00** — كما كانت.
- `admin_telegram_otps`: 3 صفوف · `telegram_auth_logs`: 0 صفًا — كما كانا.
- لم تُعدَّل أي ترحيلة قديمة.
- الثغرات المتوسطة (19) والمنخفضة (13) لم تُعالَج — خارج نطاق هذه الجولة.

---

## 8. الثغرات المتبقّية

| # | الثغرة | الخطورة | الحالة |
|---|---|---|---|
| H-01 | 2FA واجهةٌ فقط — الجلسة تُصدَر قبل العامل الثاني | **عالية** | **مفتوحة.** خطة جاهزة في `AUTH_HARDENING_PLAN.md`، تنتظر قرار المالك |
| H-08 | سرّ Turnstile ما زال في تاريخ Git | **عالية** | **مفتوحة جزئيًا.** أُزيل من الملف المنشور؛ **يجب تدويره في Cloudflare** |
| H-09 | انحراف المستودع عن الإنتاج | عالية | **مُخفَّفة لا مغلقة.** فحص CI يمنع الاتساع، والانحراف القائم مُجمَّد في `drift-baseline.json` (175 دالة قاعدة + 19 دالة حافة بلا مصدر) |
| H-10 | «البريد كسلطة» | عالية | **مسار الاختطاف مغلق ومُثبَت** (C-02 يحمي `email`). التصميم نفسه دَين قائم |
| — | 19 متوسطة + 13 منخفضة | متوسطة/منخفضة | **لم تُعالَج** — خارج نطاق هذه الجولة |

**الحرجة السبع (C-01 … C-07): كلها مغلقة ومُثبَت إغلاقها.**
**العالية العشر: 7 مغلقة، وH-01 مفتوحة، وH-08 تنتظر إجراء المالك، وH-09 مُخفَّفة.**

---

## 9. إجراءات لا يستطيع غير مالك المشروع تنفيذها

1. **تدوير سرّ Turnstile في Cloudflare.** أُزيل من الملف المنشور، لكنه باقٍ في
   تاريخ Git — ومَن استنسخ المستودع يملكه.
2. **حذف `gemini-proxy` و`huggingface-chatbot`** من لوحة Supabase. الثغرتان
   مغلقتان بالنشر (كلاهما 410)، والحذف تنظيف. (لا توجد أداة حذف في MCP.)
3. **ضبط `SUPABASE_ACCESS_TOKEN` و`SUPABASE_PROJECT_REF`** في أسرار المستودع
   حتى تعمل مقارنة الانحراف بالإنتاج فعليًا بدل أن تتخطّى بتحذير.
4. **قرار H-01** — الخيارات في `AUTH_HARDENING_PLAN.md` §7.
5. **قرار `wallet_enforce_send`** — متى يتحوّل القياس إلى حظر.

---

## 10. هل يوجد ما يمنع الإطلاق؟

**لا يوجد مانع تقني من الثغرات التي عولجت في هذه الجولة.** الحرجة السبع
مغلقة ومُثبَت إغلاقها.

**لكن ثلاثة بنود يجب حسمها قبل إطلاق عام:**

1. **تدوير سرّ Turnstile** (§9.1) — إجراء دقيقة واحدة، ومَن يملك السرّ القديم
   يستطيع تزوير اجتياز التحقق.
2. **H-01** — 2FA ميزة **معلَن عنها للمستخدم** وهي لا تحمي فعليًا. ثلاثة حسابات
   فقط تستعملها اليوم، وهذه أرخص لحظة ممكنة للإصلاح.
3. **`wallet_enforce_send`** — القياس يعمل، والحظر معطَّل بقرار. الإطلاق العام
   بلا فرض يعني إرسالًا بلا سقف على حساب Meta الخاص بالمنصة.

ولاحظ أن المنصة **في وضع ما قبل الإطلاق أصلًا**: بوابة الحساب (الترحيل 042)
تحجب كل حساب بلا هاتف مُسجَّل وتعرض رسالة قائمة انتظار بتاريخ «أكتوبر 2026».
تأكّد من ذلك أثناء الاختبار: حساب `role='user'` بلا هاتف يرى صفر تذاكر وصفر
إشعارات، و`my_account_gate()` تعيد `status: needs_phone`. **هذا سلوك مقصود لا
عُطل** — لكنه يعني أن أي اختبار قبول قبل الإطلاق يجب أن يجري على حساب اجتاز
البوابة، وإلا بدت المنصة فارغة.
