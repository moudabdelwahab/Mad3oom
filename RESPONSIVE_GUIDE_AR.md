# دليل التجاوب مع شاشات الجوال

هذا الملف يشرح طبقة التجاوب المشتركة في المشروع: ما تفعله، وكيف تبني
عليها صفحة جديدة، وكيف تتحقّق آلياً من أنك لم تكسر شيئاً.

## الملفّان الأساسيان

| الملف | الدور |
|---|---|
| `assets/responsive-global.css` | قواعد المقاسات والتخطيط للشاشات الصغيرة |
| `assets/responsive-global.js`  | يلفّ الجداول تلقائياً في حاوية تُمرَّر أفقياً |

كلاهما مربوط في كل صفحة **في آخر `<head>`** حتى يعلو على بقية ملفات
التنسيق. لا يغيّران الألوان ولا الخطوط ولا منطق أي مكوّن.

في مستودع واتساب (`whatsapp-mad3oom`) نفس الملفّين باسم
`responsive.css` و`responsive.js`، ويزيد عليهما `mobile-nav.js`
و`inbox-mobile.js`.

## صفحة جديدة؟ أضِف هذين السطرين

```html
<link rel="stylesheet" href="/assets/responsive-global.css">
<script src="/assets/responsive-global.js" defer></script>
```

ضعهما **في آخر `<head>`**، ومسارهما مطلق كبقية أصول المشروع فلا يتأثر
بعمق الصفحة. وتأكّد من وجود:

```html
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
```

`viewport-fit=cover` ضروري حتى تعمل `env(safe-area-inset-*)` على
الأجهزة ذات القصّة (notch).

## ما تتكفّل به الطبقة تلقائياً

- **الجداول**: تُلفّ في `.table-responsive` وتُمرَّر أفقياً — بما فيها
  الجداول التي يبنيها JavaScript بعد جلب البيانات (عبر `MutationObserver`).
- **أهداف اللمس**: 44×44 كحد أدنى للأزرار والأيقونات وروابط القوائم
  (توصية Apple وWCAG 2.5.5). روابط النص الجاري مستثناة عمداً — إجبارها
  على 44px يكسر الفقرة، والمعيار نفسه يستثنيها.
- **الحقول**: خط 16px على الجوال حتى لا يكبّر iOS الصفحة تلقائياً عند
  التركيز على حقل.
- **النوافذ المنبثقة**: عرض الشاشة ناقص هامش، وارتفاع بـ`dvh` لا `vh`
  (شريط المتصفح على الجوال يتغيّر ارتفاعه أثناء التمرير).
- **المناطق الآمنة**: هامش سفلي يراعي شريط الإيماءات.

## ثلاثة أخطاء تكرّرت — وكيف تتجنّبها

### 1. `min-width: auto` على عناصر الشبكات والفلكس

عنصر داخل `flex`/`grid` يرفض الانكماش دون العرض الجوهري لمحتواه. فسطر
كود طويل أو حقل إدخال يمدّ العمود ← ثم الصفحة كلها، **ويُبطل أي
`overflow-x: auto` وضعته على العنصر الداخلي** لأن الأب اتّسع أصلاً.

```css
/* في المصدر، على الحاوية: */
.my-row > * { min-width: 0; }
```

الطبقة العامة تفعل هذا على الجوال للأسماء الشائعة
(`*grid*`, `*flex*`, `*row*`, `*bar*`, `*group*` …) مع استثناء العناصر
التفاعلية حتى لا تنكمش أزرار وروابط أشرطة التنقّل.

### 2. شبكات `minmax(250px, 1fr)`

```css
/* ✗ لا ينزل العمود تحت 250px فيمدّ الصفحة على شاشة 320px */
grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));

/* ✓ */
grid-template-columns: repeat(auto-fit, minmax(min(250px, 100%), 1fr));
```

### 3. `flex-wrap: wrap` مع `flex-direction: column`

بعد قلب الاتجاه إلى عمود يصبح الالتفاف على المحور الرأسي، فيُحسب عرض
السطر من أعرض عنصر (`max-content`) بدل عرض الحاوية — فتخرج العناصر عن
الشاشة. إن قلبت الاتجاه في استعلام وسائط، اقلب `flex-wrap: nowrap` معه.

## الفحص الآلي

سكربت الفحص يحمّل كل صفحة بعرض جوال ويصنّف كل عنصر يقع خارج الشاشة:

- **scrollable** — داخل حاوية تُمرَّر أفقياً ← سلوك مقصود، يُتجاهل.
- **clipped** — يقصّه أبٌ بـ`overflow: hidden` ← المحتوى غير قابل
  للوصول = عيب حقيقي.
- **spill** — يمدّ الصفحة نفسها ← عيب حقيقي.

التمييز مهم: قياس `document.scrollWidth` وحده لا يكفي، لأن أباً
بـ`overflow: hidden` يُخفي المشكلة بدل أن يحلّها.

> السكربت غير محفوظ في المستودع (كان أداة تطوير مؤقتة). لإعادة بنائه:
> شغّل خادماً ساكناً على جذر المشروع، ثم افتح كل صفحة في Playwright
> بمقاس 375×812 وطبّق التصنيف أعلاه على `document.querySelectorAll('body *')`.

### نقطة مهمة عند الفحص

لوحات التحكم تجلب شريطها الجانبي وقت التشغيل من
`assets/components/*.html` بعد التوثيق. من دون جلسة صالحة لا يظهر الشريط
إطلاقاً، فيبدو الفحص «نظيفاً» وهو لم يختبر هيكل الصفحة الحقيقي. احقن
الجزء يدوياً قبل القياس:

```js
document.getElementById('sidebar-container').innerHTML =
  await fetch('/assets/components/customer-sidebar.html').then(r => r.text());
```

## نتيجة آخر فحص

| المقاس | Mad3oom (79 صفحة) | whatsapp-mad3oom (7 صفحات) |
|---|---|---|
| 320px | 78/79 | 7/7 |
| 375px | — | 7/7 |
| 414px | — | 7/7 |
| 768px | — | 7/7 |

الخانات المعلَّمة `—` لم يُسجَّل فيها رقم بعد؛ أعِد تشغيل الفحص عند
المقاس المطلوب لملئها.

الاستثناء المقبول الوحيد في Mad3oom هو `admin/automation.html`: مساحة
الرسم فيها 4400px داخل إطار يُسحب ويُقرَّب (pan/zoom)، وهذا أصل عمل
المحرّر لا عيب فيه.

أهداف اللمس المتبقية دون الحد كلها روابط داخل نص جارٍ
(`privacy.html`, `terms.html`, `start.html`) — وهي المستثناة صراحةً في
WCAG 2.5.5، وإجبارها على 44px يكسر الفقرة.
