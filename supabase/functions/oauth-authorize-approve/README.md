# دالة `oauth-authorize-approve` — لقطة من الإنتاج

**لقطة حرفية** للمصدر المنشور فعليًا في مشروع `srnelrdpqkcntbgudyto`،
حُفظت لأن الدالة لم تكن موجودة في المستودع.

| | |
|---|---|
| النسخة المنشورة وقت اللقطة | **15** |
| `ezbr_sha256` | `4f37ce2eb2d9ab62d807805d1cedc661d8056c0022ab092d45f9b6ea0dd058e5` |
| `verify_jwt` | **`true`** |
| عدد الملفات | 1 |
| المصدر | `mcp__Supabase__get_edge_function(project_id="srnelrdpqkcntbgudyto", function_slug="oauth-authorize-approve")` |
| تاريخ اللقطة | 2026-09-15 |

الدور: خطوة موافقة المستخدم داخل تدفّق التفويض — تُستدعى من المتصفّح
بجلسة مستخدم مسجَّل، لذلك `verify_jwt:true` (بعكس `oauth-register` و`oauth-token`).

## لم يُعدَّل أي سلوك

اللقطة حرفية عمدًا. **لم يُغيَّر** أي دومين، أو `resource`، أو ترويسة،
أو منطق مصادقة/تفويض، أو أي إعداد إنتاج.
