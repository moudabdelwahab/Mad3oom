# خطة تقوية المصادقة — H-01

**الحالة:** خطة مقترحة. **لم يُنفَّذ منها شيء.**
**التاريخ:** 2026-09-18 · **المشروع:** Mad3oom (`srnelrdpqkcntbgudyto`)
**المرجع:** `FULL_PROJECT_AUDIT.md` § H-01 · **الأولوية:** P1

> لماذا خطة لا تنفيذ: H-01 ليس خطأً في سطر يُصلَح، بل قرار معماري في **من
> يُصدِر الجلسة ومتى**. تنفيذه يمسّ كل مسار دخول في المنصة، ويُخرج كل جلسة
> قائمة من حالة «صالحة» إلى حالة تحتاج ترقية. تغيير بهذا الاتساع على قاعدة
> إنتاج حيّة يحتاج قرار مالك، لا قرار مُدقِّق.

---

## 1. الحالة القائمة — بالأدلة

### 1.1 الجلسة تُصدَر قبل العامل الثاني

في `auth-client.js` (دالة `signIn`) الترتيب هو:

1. `supabase.auth.signInWithPassword(...)` → **ينجح، ويُصدِر `access_token` +
   `refresh_token` كاملَين**، ويحفظهما في `localStorage` بحكم `persistSession`
   الافتراضي.
2. ثم — وبعدها فقط — يُقرأ `profiles` ويُقرَّر:

```js
if (profile.two_factor_enabled) { … return { data: result.data, requires2FA: true, profile }; }
if (profile.telegram_otp_enabled && profile.telegram_chat_id) { … return { … requiresTelegramOTP: true … }; }
```

`requires2FA` و`requiresTelegramOTP` **علامتان للواجهة**. لا شيء في الخادم
يعرف بوجودهما، ولا شيء يمنع حامل التوكن من تجاهل الشاشة التالية.

### 1.2 دالة التحقق لا تُصدِر ولا تُرقّي شيئًا

`verify-2fa` مُحكَمة في ما تفعله: تحلّ الهوية من التوكن عبر `/auth/v1/user` لا
من `sub` غير الموثّق، وتقبل السرّ المخزَّن وحده، وفيها خنق حقيقي
(`MAX_ATTEMPTS = 5`، نافذة 10 دقائق، قفل 15 دقيقة). لكن ردّها النهائي هو:

```ts
return new Response(JSON.stringify({ verified, enrollment: isEnrollment || undefined }), …)
```

`{verified: true}` **وحسب**. لا جلسة جديدة، ولا ادّعاء (claim) مضاف، ولا صفّ
يقول «هذا المستخدم أتمّ العامل الثاني في هذه الجلسة». و`finalizeLogin()` في
الواجهة تكتفي بالتوجيه إلى لوحة التحكم.

نفس الشيء بعد إصلاح H-02: `verify-otp` صارت تتحقق من الهوية وتخنق التخمين
وتسجّل، لكنها **ما زالت** تُرجع `{success: true}` على جلسة صادرة سلفًا. أي أن
إصلاح H-02 أغلق تجاوز طبقة OTP، ولم يغلق H-01 — وهذا مقصود وموثَّق.

### 1.3 سيناريو الاستغلال

مهاجم يملك كلمة المرور:

```
POST /auth/v1/token?grant_type=password   → access_token + refresh_token
GET  /rest/v1/<أي جدول>                   → يعمل
POST /rest/v1/rpc/<أي دالة>               → يعمل
POST /functions/v1/<أي دالة>              → يعمل
```

لا يحتاج أن يفتح شاشة الدخول أصلًا. ولو فتحها، يكفيه أن يتوقف عند خطوة الرمز
ويكتب العنوان يدويًا. **لا طبقة خادم واحدة تسأل: هل مرّ هذا التوكن بالعامل
الثاني؟**

### 1.4 الأثر الفعلي اليوم

| المقياس | القيمة على Production (2026-09-18) |
|---|---|
| إجمالي الحسابات | 32 |
| حسابات بـ‎`two_factor_enabled`‎ | 1 |
| حسابات بـ‎`telegram_otp_enabled`‎ | 2 |

العدد صغير الآن — وهذه بالضبط **اللحظة المناسبة** للتغيير: كلفة الترحيل تتناسب
طرديًا مع عدد الحسابات المُفعَّلة، وهي اليوم ثلاثة.

⚠️ لكن الخطورة ليست في العدد: 2FA **ميزة معلَن عنها للمستخدم**. المستخدم الذي
فعّلها يظن أن كلمة مروره وحدها لا تكفي — وهي تكفي. هذه فجوة بين الوعد والواقع،
وهي أسوأ من غياب الميزة أصلًا.

---

## 2. الخيارات المطروحة

### الخيار أ — MFA الأصلي في Supabase (موصى به)

استعمال `supabase.auth.mfa.*` (enroll / challenge / verify). عند نجاح العامل
الثاني يرفع GoTrue ادّعاء `aal` في الـJWT من `aal1` إلى `aal2`.

**الربح الحاسم:** الشرط يصير قابلًا للفرض في **قاعدة البيانات**، لا في الواجهة:

```sql
-- مثال توضيحي فقط — لا يُطبَّق في هذه المرحلة
create policy "aal2 required for billing"
  on public.wa_wallets for select
  using ( (auth.jwt()->>'aal') = 'aal2' );
```

وهذا هو الفارق الجوهري: العامل الثاني ينتقل من «شاشة» إلى **شرط على الصف**.

| الإيجابيات | السلبيات |
|---|---|
| الادّعاء يصدره GoTrue لا كودنا — لا يمكن تزويره | يتطلب إعادة تسجيل (re-enroll) لكل مستخدم مُفعِّل حاليًا |
| `aal2` مفروض في RLS وفي كل دالة دفعة واحدة | السرّ الحالي في `profiles.two_factor_secret` لا ينتقل تلقائيًا إلى `auth.mfa_factors` |
| تدوير التوكن والجلسات يديره Supabase | `verify-2fa` و`generate-2fa-secret` و`disable-2fa` تصير زائدة ويجب سحبها بترتيب |
| لا يوجد مسار OTP تيليجرام في MFA الأصلي — يحتاج معالجة منفصلة (انظر §5) | |

### الخيار ب — تأجيل إصدار الجلسة (نمط `pi-auth`)

النمط **مطبَّق ويعمل في هذا المشروع بالفعل** — في `pi-auth`:

```ts
const { data: link } = await admin.auth.admin.generateLink({ type: "magiclink", email });
// لا access_token ولا refresh_token هنا: العميل يبدّل token_hash بجلسة
return json({ token_hash: link.properties.hashed_token, … });
```

التطبيق على الدخول العادي:

1. الواجهة تستدعي `signInWithPassword` → **ثم `signOut()` فورًا** قبل أي شيء.
2. تُرسل كلمة المرور + الرمز إلى دالة حافة واحدة (`login-with-2fa`).
3. الدالة تتحقق من الاثنين، ثم `generateLink` وتعيد `token_hash` فقط.
4. الواجهة تُبدّل `token_hash` بجلسة عبر `verifyOtp({ type: 'magiclink' })`.

| الإيجابيات | السلبيات |
|---|---|
| لا إعادة تسجيل: السرّ يبقى حيث هو | `signInWithPassword` يظل يُصدِر جلسة للحظة — نافذة ضيقة لكنها ليست صفرًا |
| النمط مُختبَر داخل نفس المشروع | لا يوجد `aal` — لا يمكن اشتراطه في RLS |
| يغطي TOTP وOTP تيليجرام بنفس الشكل | منطق المصادقة يصير كودًا نكتبه نحن ونصونه |

### الخيار ج — عدم التغيير مع إزالة الوعد

إبقاء المعمارية وسحب 2FA من الواجهة، وتعليم الحسابات الثلاثة بأنها غير محمية
بعامل ثانٍ.

هذا **ليس إصلاحًا**، لكنه أمين: يزيل الفجوة بين ما يُوعَد به المستخدم وما
يحصل عليه فعلًا. يُذكر هنا لأنه الخيار الوحيد بلا كلفة ترحيل، ولأن تركه
مجهولًا أسوأ من ذكره.

**التوصية:** الخيار (أ). والسبب الوحيد الكافي: هو الخيار الذي يجعل العامل
الثاني **مفروضًا في قاعدة البيانات**. أي حلٍّ يبقي الفرض في الواجهة أو في كود
نكتبه سيُنسى في أول مسار جديد.

---

## 3. المخاطر

| # | الخطر | الاحتمال | الأثر | التخفيف |
|---|---|---|---|---|
| R1 | **قفل المستخدمين خارج حساباتهم** أثناء الترحيل | متوسط | حرج | مسار استرداد يديره الأدمن + إبقاء الدخول بلا عامل ثانٍ عاملًا حتى اكتمال إعادة التسجيل |
| R2 | إبطال كل الجلسات القائمة عند التبديل | مرتفع | متوسط | إعلان نافذة صيانة؛ العدد 32 حسابًا فقط |
| R3 | ضياع `two_factor_secret` عند الانتقال لـ`auth.mfa_factors` | متوسط | مرتفع | **لا يُحذف العمود القديم في نفس الإصدار**؛ يُحتفظ به للقراءة فقط دورةً كاملة |
| R4 | اشتراط `aal2` في RLS يكسر مسارات آلية (مفاتيح API، MCP، Webhooks) | **مرتفع** | **حرج** | هذه المسارات لا تملك `aal2` إطلاقًا ولا يمكنها امتلاكه. أي سياسة `aal2` يجب أن تستثني مسار الخدمة صراحةً |
| R5 | OTP تيليجرام لا مكان له في MFA الأصلي | مؤكد | متوسط | يُعامَل كمسار منفصل (§5) أو يُسحب |
| R6 | تعطُّل تسجيل الدخول للأدمن أثناء النشر | منخفض | حرج | النشر خلف علم تشغيل + حساب أدمن واحد خارج الشرط حتى التأكيد |

**R4 هو الخطر الأخطر ويستحق تكرارًا:** المنصة فيها مفاتيح API وخادم MCP و
webhooks تعمل بلا مستخدم بشري. `aal` لا يوجد في توكناتها أصلًا. أي سياسة
`(auth.jwt()->>'aal') = 'aal2'` تُكتب بلا استثناء صريح لمسار الخدمة **ستقطع
الخدمة عن العملاء**. هذا وحده سبب كافٍ لعدم تطبيق أي سياسة `aal2` في نفس
الإصدار الذي يُفعِّل MFA.

---

## 4. خطة الترحيل المقترحة

> كل مرحلة قابلة للتراجع وحدها. لا مرحلة تبدأ قبل التحقق من سابقتها على
> Production.

### المرحلة 0 — قياس (بلا أي تغيير سلوكي)
- تسجيل كل تسجيل دخول مع `aal` الفعلي و«هل مرّ بعامل ثانٍ؟».
- **معيار الخروج:** أسبوع من البيانات يحدد بدقة من سيتأثر.

### المرحلة 1 — تفعيل MFA بالتوازي
- تفعيل `auth.mfa` في المشروع.
- إضافة مسار «فعّل 2FA بالطريقة الجديدة» في الإعدادات، بلا إجبار.
- المسار القديم يبقى عاملًا كما هو.
- **معيار الخروج:** حساب اختبار واحد يُسجّل ويدخل بنجاح، ويحمل توكنه `aal2`.

### المرحلة 2 — إعادة تسجيل الحسابات الثلاثة
- تواصل مباشر مع أصحاب الحسابات الثلاثة (العدد يسمح بذلك).
- إعادة تسجيل مع مسار استرداد جاهز ومُختبَر **قبل** البدء.
- **معيار الخروج:** 3/3 مُسجَّلة بالطريقة الجديدة ودخلت بنجاح مرة واحدة على الأقل.

### المرحلة 3 — إصدار الجلسة مشروط
- `signInWithPassword` يعقبه `signOut()` فورًا حين يكون للحساب عامل ثانٍ.
- الجلسة الحقيقية لا تُصدَر إلا بعد `mfa.verify`.
- **معيار الخروج:** محاولة استعمال التوكن بين الخطوتين تُرفض — يُختبر صراحةً.

### المرحلة 4 — الفرض في القاعدة (الأخطر — مستقل تمامًا)
- سياسات `aal2` على الجداول الحسّاسة وحدها (المحافظ، مفاتيح API، بيانات الاعتماد).
- **كل سياسة تستثني مسار الخدمة صراحةً** (`auth.uid() is null`) وإلا انقطعت
  مسارات المفاتيح وMCP (R4).
- **معيار الخروج:** مفتاح API حيّ ينجح، ومستخدم `aal1` يُرفض — الاثنان مُثبتان
  باختبار داخل transaction يُتراجَع عنه.

### المرحلة 5 — السحب
- تعطيل `verify-2fa` و`generate-2fa-secret` و`disable-2fa` (ردّ 410 كما فُعل
  مع `gemini-proxy` و`huggingface-chatbot`).
- حذف `profiles.two_factor_secret` **بعد دورة كاملة** من استقرار المرحلة 4، لا قبلها.

---

## 5. مسار OTP تيليجرام

MFA الأصلي في Supabase لا يعرف تيليجرام. أمامه ثلاثة مصائر:

1. **سحبه** والاكتفاء بـTOTP. الأبسط، ويقلّل مساحة الهجوم.
2. **إبقاؤه بنمط الخيار (ب)** — إصدار الجلسة من دالة حافة بعد التحقق. يتعايش
   مع MFA الأصلي لكن بلا `aal2`.
3. **ترقيته إلى Phone Factor** في MFA الأصلي عبر مزوّد رسائل. أنظف، وأغلى.

القرار منتجيّ لا أمنيّ، وبياناته: حسابان يستعملانه، و`telegram_auth_logs`
**فارغ تمامًا** — أي أن المسار لم ينجح ولا مرة منذ إنشائه (السبب مذكور في §6).

---

## 6. ما أُصلح فعلًا في هذه الجولة (خارج نطاق هذه الخطة)

هذه الخطة عن H-01 وحده. أما **H-02 فقد نُفِّذ بالكامل** لأنه إصلاحات صغيرة
وواضحة لا معمارية:

- الهوية في `verify-otp` صارت من توكن الجلسة لا من جسم الطلب.
- فحص المحاولات انتقل **قبل** مطابقة الهاش (كان بعدها، فلم يكن يخنق شيئًا).
- أُضيف حدّ لكل IP.
- سياسة القراءة على `admin_telegram_otps` أُسقطت: كان صاحب الحساب يقرأ
  `otp_hash` الخاص به، وهو SHA-256 لستة أرقام يُكسر محليًا في أجزاء من الثانية.
  **هذا اكتشاف جديد أثناء الإصلاح لم يكن في التقرير الأصلي.**
- **عُطل قائم اكتُشف بالمناسبة:** الواجهة كانت ترسل `{ userId, code }` والدالة
  تقرأ `{ userId, otp }` — فكان كل تحقق يخرج بـ400 "Missing data". و
  `telegram_auth_logs` الفارغ يؤكد أن المسار لم ينجح ولا مرة. أُصلح الطرفان.

التفاصيل والأدلة في `PRODUCTION_SECURITY_HARDENING_REPORT.md`.

---

## 7. القرار المطلوب من مالك المشروع

1. **الخيار: أ أم ب أم ج؟** (التوصية: أ)
2. **مصير OTP تيليجرام؟** (§5)
3. **هل تُنفَّذ المرحلة 4 — فرض `aal2` في RLS؟** وهي منفصلة تمامًا عن باقي
   الخطة ويمكن رفضها وحدها. تحذير R4 ينطبق عليها بالكامل.
4. **نافذة الصيانة** لإبطال الجلسات (R2).

لا شيء مما سبق يُنفَّذ قبل هذا القرار.
