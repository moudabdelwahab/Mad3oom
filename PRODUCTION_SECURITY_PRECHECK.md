# Production Security Pre-Check

**المشروع:** Mad3oom — Supabase `srnelrdpqkcntbgudyto` (PostgreSQL 17.6)
**التاريخ:** 2026‑09‑18
**الغرض:** إثبات الحالة الفعلية للإنتاج **قبل** تطبيق أي إصلاح، وتحديد ما ينقص بالضبط لكل ترحيل مستهدف.
**طريقة الفحص:** استعلامات قراءة فقط على كتالوج النظام (`pg_proc`, `pg_policies`, `pg_trigger`, `information_schema`, `storage.*`)، وفحوص قدرة داخل `BEGIN … ROLLBACK`. **لم تُكتب أي بيانات ولم يُعدَّل أي كائن.**

---

## 0. أهم نتيجة: سجل الترحيلات غير صالح كمصدر حقيقة

`supabase_migrations.schema_migrations` يقفز من `20260907134626 revoke_trigger_function_execute` (=022) مباشرةً إلى `20260909220633 company_customer_ticket_separation_033` (=033).

أي أن الترحيلات **023 · 024 · 025 · 027 · 028 · 029 · 030 · 031 · 032 غير مسجَّلة إطلاقًا** في السجل.

ومع ذلك أثبت الفحص الكائني أن **024 و025 و027§1 و029§1 و031 و032 مطبَّقة فعلًا** (`is_platform_staff` و`owned_feature_keys` و`is_admin_user` موجودة). أي أنها طُبِّقت عبر محرّر SQL أو نداء مباشر لا يسجّل في الجدول.

**ثلاث نتائج تشغيلية تتبع ذلك:**

1. `supabase db push` **غير آمن هنا**: سيحاول إعادة تطبيق ملفات يظنّها ناقصة، ومنها ما يَجُبّ إصلاحات أحدث (انظر §2 أدناه).
2. «الترحيل رقم N غير مطبَّق» عبارة لا معنى لها في هذا المشروع. الوحدة الصحيحة هي **الكائن** لا الملف.
3. كل قرار في هذه الدفعة مبنيّ على فحص كائني مباشر، لا على السجل.

---

## 1. الحالة الفعلية لكل ترحيل مستهدف

### 023 — Subscription purchase RPC · **غير مطبَّق بالكامل**

| الكائن | الحالة |
|---|---|
| `request_subscription_purchase(text,text,uuid,boolean,text,text)` | ❌ غير موجودة |
| `cancel_my_subscription_request(uuid)` | ❌ غير موجودة |
| سياسة `Users can create their own subscriptions` (يجب أن تُحذف) | ⚠️ **موجودة** |
| `enforce_subscription_purchase_rules()` يرفض `status <> 'pending'` | ❌ النسخة المنشورة هي نسخة 017 وتسمح بـ`'active'` |
| تعليق الجدول | ❌ غير مضبوط |

**اكتشاف إضافي مهم جدًا:** الواجهة الحيّة على `main` تنادي هاتين الدالتين بالفعل:

```
whatsapp-subscription-service.js:353   .rpc('request_subscription_purchase', {...})
whatsapp-subscription-service.js:390   .rpc('cancel_my_subscription_request', {...})
```

ولا يوجد في المستودع كله أي `.insert()` مباشر على `whatsapp_subscriptions`.

⇒ **مسار شراء الاشتراك مكسور في الإنتاج الآن** (نداء لدالة غير موجودة ⇒ 404 من PostgREST). تطبيق 023 **يُصلح عطلًا قائمًا** ولا يكسر شيئًا، وحذف سياسة الإدراج لا يقطع أي مسار مستعمَل.

**المخاطرة:** منخفضة. **القرار:** يُطبَّق الملف كاملًا.

---

### 023 — تبعيات مُتحقَّق منها

`subscription_plans` ✅ · `subscription_purchase_check(text,boolean,uuid)` ✅ · الباقات الفعّالة: `support, whatsapp, bundle` ✅

---

### 027 — Legacy security closure · **مطبَّق جزئيًا — وتطبيقه كاملًا يُحدث ارتدادًا أمنيًا**

| القسم | الكائن | الحالة |
|---|---|---|
| §1 | `is_admin` · `is_support_user` · `is_platform_staff` · `is_admin_user` · `is_landing_admin` | ✅ مطبَّق — **لكن بنسخة 040 الأحدث** |
| §2 | `guard_profile_protected_columns()` + محفّزه | ❌ **غير موجود** |
| §2 | `guard_profile_protected_columns_insert()` + محفّزه | ❌ **غير موجود** |
| §5 | سياسات الرفع الثلاث على `storage.objects` | ❌ غير مطبَّقة — **3 سياسات رفع بلا قيد مسار** |

#### ⛔ حاجز: لا يجوز تطبيق 027 كملف كامل

`027 §1` يحتوي `create or replace` لأربع دوال سلطة، والإنتاج يحمل منها **نسخة 040 الأحدث**. إعادة تشغيل الملف تَجُبّ الأحدث بالأقدم:

| الدالة | الإنتاج (040 — الصحيح) | 027 §1 (أقدم) | أثر الارتداد |
|---|---|---|---|
| `is_admin()` | `role='admin' or owner_capability('admin')` | `role='admin' or is_main_admin()` | يكسر نموذج السياق: المالك يصير أدمن بلا اشتراط سياق |
| `is_support_user()` | `… or owner_capability('staff')` | `… or is_main_admin()` | نفس الارتداد |
| `is_platform_staff()` | `… or owner_capability('staff')` | `… or is_main_admin()` | نفس الارتداد |
| `is_admin_user(uuid)` | يقرأ `platform_authority` | **يقرأ `u.email in ('support@…','info@…')`** | **يُعيد إدخال البريد كآلية تفويض — وهو بالضبط ما أزاله 040** |

**القرار:** يُطبَّق من 027 **§2 و§5 فقط**، منقولان حرفيًا من الملف بلا تعديل حرف. §1 و§3 و§4 تُترك لأن الإنتاج يحملها بنسخة أحدث. §6 (التحقق الذاتي) تُترك لأنها ستفشل على أي حال: ما زالت سياسات تقارن بريدًا على نطاق `.online` (البند H‑10 في تقرير المراجعة، خارج نطاق هذه الدفعة).

هذا ليس تعديلًا على الترحيل ولا إعادة كتابة لحلّ: هو تطبيق للأجزاء الناقصة من نفس الملف بنصّها.

#### مسارات مشروعة تُكتب فيها الأعمدة المقفولة — فُحصت قبل القرار

| الدالة | تكتب | تعمل بـ`auth.uid()`؟ | هل يمنعها الحارس؟ |
|---|---|---|---|
| `expire_stale_subscriptions()` | `whatsapp_enabled` | لا — pg_cron | ❌ لا (فرع `auth.uid() is null`) |
| `recompute_user_access(uuid)` | `whatsapp_enabled` | نعم، لكن `revoke all … from authenticated` ومنادوها كلهم دوال أدمن | ❌ لا (فرع `is_admin()`) |
| `aqar_set_enabled()` | لا يكتب عمودًا مقفولًا | — | ❌ لا |

⇒ **صفر مسار مشروع ينكسر.**

---

### 028 — Storage closure · **غير مطبَّق بالكامل**

| الكائن | الحالة |
|---|---|
| `can_access_ticket(uuid)` | ❌ غير موجودة |
| `storage_ticket_id(text)` | ❌ غير موجودة |
| سياسات SELECT على `storage.objects` | ❌ **صفر سياسة** |
| سياسة `ticket_attachments` تستعمل `can_access_ticket` | ❌ لا |

**ملاحظة على التصميم:** قسما 4 و5 في الملف **معطَّلان عمدًا** (مُعلَّقان) — الأول ترحيل البيانات والثاني قلب المستودعات، وكلاهما يقع في 030. فتطبيق 028 وحده لا يقلب شيئًا ولا يكسر رابطًا.

**أثر تغيير سياسة `ticket_attachments`:** توسيع مقصود من «صاحب التذكرة وحده» إلى `can_access_ticket` = صاحب التذكرة **أو** الطاقم **أو** مالك الشركة على تذاكر أعضائه — أي مطابقة `tickets_select_policy` القائمة. يُصلح تعارضًا حاليًا (التذكرة مقروءة والمرفق لا).

**المخاطرة:** منخفضة. **القرار:** يُطبَّق الملف كاملًا.

---

### 029 — Entitlement & points guard · **مطبَّق جزئيًا**

| القسم | الكائن | الحالة |
|---|---|---|
| §1 | `has_chatbot_entitlement(uuid)` | ✅ موجودة وخالية من `super_user` — **بنسخة مختلفة عن الملف** |
| §2 | `guard_profile_points_change()` | ❌ الإنتاج يحمل **نسخة 008** التي ما زالت تثق بعلَم `app.bypass_profile_points_guard` |
| §3 | `is_banned(uuid)` | ❌ غير موجودة |

**§1 — قرار الترك:** نسخة الإنتاج `(whatsapp_enabled = true or role = 'admin')` **أضيق** من نسخة الملف `(… or role in ('admin','support'))`. الهدف الأمني (نزع `super_user`) محقَّق بالفعل، وتطبيق الملف كان سيوسّع الاستحقاق ليشمل `support` بلا سبب أمني. تُترك كما هي.

**§2 — يُطبَّق:** نسخة 029 تسأل عن السلطة (`is_admin()`) بدل الثقة بعلَم يضبطه المتصل. العلَم اليوم غير مستغَل (أُثبت في الملف نفسه) لكنه لغم.

**§3 — يُطبَّق:** `is_banned()` تُعرَّف. وربطها بالبوابة قرار منتج تركه الترحيل عمدًا — وقد طلبه المستخدم صراحةً في هذه الدفعة، فسيُنفَّذ كخطوة موثَّقة منفصلة.

**أثر ربط الحظر — مقيس:** كل الصفوف الـ32 في `profiles` تحمل `ban_status='none'`، و`ban_until` فارغ في كلها، و`is_locked=false` في كلها.
⇒ **صفر مستخدم يُحجَب اليوم** نتيجة الربط.

---

### 030 — Storage privatisation · **غير مطبَّق بالكامل**

| الكائن | الحالة |
|---|---|
| عمود `ticket_attachments.file_path` | ❌ غير موجود |
| `tickets` / `chat-attachments` خاصّان | ❌ الاثنان **عامّان** |
| سياسات الكتابة مقيَّدة بالمسار | ❌ 3 سياسات رفع بلا قيد |

#### الشروط المسبقة — كلها مُتحقَّق منها

1. **الواجهة توقّع الروابط بالفعل، وهي منشورة على `main`:**
   - `storage-urls.js` — `createSignedUrl` / `createSignedUrls` + `toObjectPath` يفهم الرابط القديم والمسار الجديد
   - `tickets-service.js:833, 859` — يخزّن `file_path` ويوقّع عند العرض
   - `assets/js/chat-logic.js:454` — `signedUrls(CHAT_ATTACHMENTS_BUCKET, …)` عند العرض، ويخزّن المسار لا الرابط
2. **الترحيل البياني يمرّ:** تشغيل جاف لاستعلام 030 نفسه — **6 صفوف من 6 تطابق كائنات موجودة فعلًا** في مستودع `tickets`. تحقُّق 030 الذاتي (`raise exception` عند صف بلا ملف) سيمرّ.
3. **أشكال المسارات مدعومة:** العرفان القائمان معًا في `tickets` — `<ticket_id>/file` (4 كائنات) و`<user_id>/<ticket_id>/file` (2 كائنان) — وكلاهما يفكّه `storage_ticket_id()` صراحةً.
4. **صلاحية التنفيذ مثبتة:** المنادي `postgres` ليس مالك `storage.objects` (المالك `supabase_storage_admin`)، لكن فحصًا داخل `BEGIN … ROLLBACK` أثبت أن `CREATE POLICY` عليه **ينجح**. فالأقسام الملفوفة بـ`exception when insufficient_privilege` لن تُتخطّى صامتة.

   > لولا هذا الفحص لكان الخطر حقيقيًا: تخطّي صامت لسياسات القراءة **مع** نجاح قلب المستودع كان سيجعل كل المرفقات غير مقروءة للجميع.

#### أثر متبقٍّ واحد — موثَّق ومقبول

المستودع `chat-attachments` يحوي 4 كائنات، **كلها تحت `chat-media/…`** أي أن أول مقطع ليس معرّف مستخدم. وسياسة القراءة في 028 تشترط `is_platform_staff()` أو `foldername[1] = auth.uid()`.

⇒ بعد القلب تبقى هذه الأربعة مقروءة **للطاقم فقط**.

وقياس الأثر الفعلي:
- الكائنات الأربعة كلها مملوكة للحساب `dcd13822…` وهو **حساب أدمن**.
- `chat_messages` فيه **صفر صف** يحمل `image_url` غير فارغ.

⇒ **لا عميل يفقد صورة**، ولا رسالة واحدة تتأثر.

#### التراجع
`update storage.buckets set public = true where id in ('tickets','chat-attachments');` — فوري، بلا حركة ملفات وبلا فقد بيانات.

**المخاطرة:** منخفضة ومقيسة. **القرار:** يُطبَّق الملف كاملًا، بعد 028.

---

### 012 — Customer reopen ticket · **مطبَّق جزئيًا، والباقي يحتاج قرارًا**

| الكائن | الحالة |
|---|---|
| `restrict_customer_ticket_update()` | ✅ موجودة |
| `reopen_ticket_on_owner_reply()` | ❌ غير موجودة |
| محفّز `trg_reopen_ticket_on_owner_reply` على `ticket_replies` | ❌ غير موجود |

والسجل **يُظهر** `20260905052955 customer_reopen_ticket` كمطبَّق، ويُظهر بعده `20260909221143 explicit_ticket_reopen_and_close_034`.

**الفرضية:** 034 (إعادة الفتح والإغلاق الصريحان) جبَّ الفتح التلقائي عند ردّ المالك عمدًا. و034 **مطبَّق** (`close_ticket_in_my_scope` و`reopen_ticket_in_my_scope` موجودتان).

⇒ **لا يُعاد تطبيق 012 قبل إثبات هذه الفرضية.** إعادة المحفّز قد تُعيد سلوكًا أُزيل بقصد، وتتعارض مع نموذج 034. يُفحص نصّ 034 أولًا، والنتيجة تُوثَّق في تقرير التنفيذ. وهذا بند وظيفي لا أمني، فتأجيله بلا كلفة أمنية.

---

## 2. ثغرات ليس لها إصلاح جاهز في المستودع

| ID | الثغرة | الحالة | الإجراء المخطط |
|---|---|---|---|
| C‑03 | تصعيد نطاقات `api_tokens` بـPATCH مباشر | لا ترحيل يعالجها (036 يحرس الإصدار لا التعديل) | **ترحيل جديد** — محفّز `BEFORE UPDATE` يحرس `scopes` وما يتبعها |
| C‑06 | XSS مخزَّن في لوحة الأخطاء عبر `site_errors.status` | لا إصلاح موجود | **ترحيل جديد** (قيد `CHECK` + تضييق سياسة الإدراج) **+ هروب في الواجهة** |
| C‑07 | `gemini-proxy` منشورة وقابلة للاستغلال | لا مصدر لها في المستودع | تحييد + حذف — انظر §3 |
| H‑03 | إرسال واتساب بلا خصم | البنية موجودة (`wa_wallet_charge_message`) والمسار لا يستعملها | ربط المسار بالبنية القائمة، بوضع **قياس أولًا** |
| H‑04 | الحظر غير منفَّذ | `is_banned()` من 029 §3 + ربطها بالبوابة | 029 §3 + ربط موثَّق |

---

## 3. قيد أداة معروف — حذف دوال الحافة

واجهة Supabase MCP المتاحة في هذه الجلسة تحتوي `deploy_edge_function` و`list_edge_functions` و`get_edge_function`، **ولا تحتوي `delete_edge_function`**.

⇒ لا أستطيع حذف `gemini-proxy` من الإنتاج بنفسي.

**الخطة:** تحييدها فورًا بنشر نسخة بديلة ترفض كل طلب بـ`410 Gone` بلا أي وصول لقاعدة البيانات (نفس نمط `ai-probe-temp` القائم في المشروع)، مع أرشفة نصّها الحالي في المستودع أولًا، ثم يحذفها المالك من لوحة التحكم. التحييد يغلق الثغرة فورًا؛ الحذف يُكمل التنظيف.

---

## 4. ترتيب التطبيق المعتمد

مبنيّ على التبعيات الفعلية لا على أرقام الملفات:

| # | الإجراء | يعتمد على | المخاطرة |
|---|---|---|---|
| 1 | **023** كاملًا | `subscription_purchase_check` ✅ | منخفضة — يُصلح عطلًا قائمًا |
| 2 | **027 §2** (حارس أعمدة الملف) | `is_admin()` ✅ | منخفضة — صفر مسار مشروع يتأثر |
| 3 | **027 §5** (تقييد الرفع بالمسار) | — | منخفضة — يغلق C‑05 |
| 4 | **029 §2 + §3** | `is_admin()` ✅ | منخفضة |
| 5 | **028** كاملًا | `is_platform_staff()` ✅ | منخفضة — لا يقلب مستودعًا |
| 6 | **030** كاملًا | 028 | **منخفضة ومقيسة** — أهم خطوة مرئية للمستخدم |
| 7 | ترحيل جديد: حارس `api_tokens` (C‑03) | — | منخفضة |
| 8 | ترحيل جديد + واجهة: `site_errors` (C‑06) | — | منخفضة |
| 9 | ربط `is_banned()` بالبوابة (H‑04) | 029 §3 | منخفضة — صفر مستخدم محظور اليوم |
| 10 | تحييد `gemini-proxy` (C‑07) | — | منخفضة |
| 11 | فوترة واتساب — وضع القياس (H‑03) | `wa_wallet_charge_message` ✅ | متوسطة — تُنشر خلف علَم |

كل خطوة يتبعها تحقُّق كائني ثم اختبار سلوكي داخل `BEGIN … ROLLBACK` بهوية مستخدم حقيقي غير مميّز.

---

## 5. قرارات المستخدم المعتمدة في هذه الدفعة

1. **فوترة واتساب (H‑03):** *قياس أولًا ثم حظر*. يُربط الخصم ويُسجَّل كل إرسال، ونقص الرصيد يُسجَّل تحذيرًا ولا يمنع — خلف علَم `wallet_enforce_send` قابل للتفعيل لاحقًا بصفّ واحد. السبب: مستأجران من ثلاثة بلا رصيد، والحظر الفوري كان سيقطع خدمتهما.
2. **خصخصة التخزين (C‑04):** *تُطبَّق الآن* مع بقية الدفعة، مع تحقُّق فوري بعدها وتراجع جاهز.

---

## 6. ما لم يُفحص في هذه الدفعة

- مخطط `emp_ops` بالكامل.
- 17 دالة حافة منشورة بلا مصدر في المستودع (فُحصت منها `gemini-proxy` و`integrations-api` فقط).
- متغيرات البيئة (`META_APP_SECRET` وغيرها) — غير مقروءة، فالحكم على H‑06 يبقى «الكود مؤكَّد، البيئة مجهولة».
- الملفات HTML الضخمة سطرًا بسطر.
