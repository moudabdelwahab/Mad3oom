# مدوّنة المنصة — دليل التشغيل والتحرير

## ما الذي أُضيف

| المسار | ما هو |
|---|---|
| `/blog/` | فهرس المدوّنة العام: بحث، تصنيفات، وسوم، مقال مميّز، ترقيم |
| `/blog/post.html?slug=…` | قارئ المقال: فهرس محتويات، شريط تقدّم، مقالات ذات صلة، مشاركة |
| `/admin/blog.html` | لوحة التحرير — من **لوحة الإدارة أو لوحة المالك فقط** |
| `migrations/043_blog.sql` | الجداول والسياسات والدوال |

---

## ① خطوة إلزامية قبل أي شيء: تطبيق الترحيل

المدوّنة لا تعمل قبل تطبيق `migrations/043_blog.sql` على قاعدة Supabase.
قبله تعرض الصفحة «تعذّر تحميل المقالات» لأن الجدول غير موجود أصلًا.

```bash
psql "$SUPABASE_DB_URL" -f migrations/043_blog.sql
```

أو من محرّر SQL في لوحة Supabase: الصق محتوى الملف ونفّذه.

الترحيل **إضافي بالكامل**: لا يمسّ جدولًا قائمًا ولا سياسة قائمة، ويزرع ستة
تصنيفات جاهزة ولا يزرع أي مقال.

للتحقّق من سلامته على Postgres حقيقي:

```bash
npm run test:sql        # يشمل tests/sql/blog.test.sql
```

---

## ② من يستطيع التحرير

الكتابة محصورة في `public.is_platform_staff()` — وهي نفس الدالة التي تحرس
بقيّة لوحة الإدارة بعد الترحيل 040:

```
role IN ('admin','support)  ⋁  owner_capability('staff')
```

عمليًا:

| الحساب | القراءة | الكتابة |
|---|---|---|
| زائر بلا حساب | المنشور فقط | ✗ |
| عميل / مستخدم عادي | المنشور فقط | ✗ |
| مالك شركة (`company_admin`) | المنشور فقط | ✗ |
| `admin` / `support` | كل شيء | ✓ |
| مالك المنصة **داخل واجهة المالك أو الإدارة** | كل شيء | ✓ |
| مالك المنصة **في واجهة العميل أو الشركة** | المنشور فقط | ✗ |

السطر الأخير ليس تفصيلًا: السياق **مُرشِّح لا مصدر**، فمن خرج من واجهته
ضيّق على نفسه ولم يُصعِّد. وكل صفوف هذا الجدول مُختبَرة فعليًا في
`tests/sql/blog.test.sql`.

**لا يوجد فحص صلاحية في المتصفح.** إخفاء زرّ ليس أمانًا؛ القرار كله في
سياسات RLS، والواجهة تعرض ما أعادته القاعدة.

---

## ③ التحرير اليومي

من القائمة الجانبية: **المدوّنة** (تظهر في لوحة الإدارة وفي لوحة المالك).

### الحالات الأربع

| الحالة | من يراها | ملاحظة |
|---|---|---|
| **مسودّة** | الطاقم فقط | احفظ متى شئت — النشر خطوة منفصلة |
| **مجدول** | الطاقم فقط حتى موعده | منشور بتاريخ مستقبلي؛ يظهر وحده بلا مهمة دورية |
| **منشور** | الجميع | ظاهر على الإنترنت فورًا |
| **مؤرشف** | الطاقم فقط | يخرج من الفهرس ويبقى الرابط يعمل للطاقم |

«مجدول» ليست حالة في القاعدة بل **مشتقّة**: مقال `published` تاريخه لم يحن.
ولذلك تعمل الجدولة بلا أي مهمة مجدولة تُشغَّل ولا نقطة فشل جديدة.

### ما تحسبه القاعدة ولا تكتبه أنت

زمن القراءة، عدد الكلمات، تاريخ أول نشر، توحيد الوسوم، عدّاد المشاهدات —
كلها محفّزات في القاعدة. فالرقم الذي يراه القارئ واحد مهما كان مصدر الكتابة.

### تنسيق المتن

```
## عنوان قسم          ### عنوان فرعي
- عنصر قائمة          1. عنصر مرقّم
> اقتباس              ---  (فاصل)
**غامق**  _مائل_  `كود`
[نص الرابط](https://…)
![وصف الصورة](https://…)
```<br>كتلة كود<br>```
```

**أي وسم HTML يُعرض كنصّ ولا يُنفَّذ.** المتن يمرّ بمُحوِّل يهرب كل شيء
ويكتب الوسوم بنفسه، والروابط تُفلتَر بالمخطَّط (`javascript:` و`data:`
مرفوضان). المعاينة في اللوحة تستعمل **نفس** المُحوِّل، فما تراه هو ما يُنشر.

### الرابط المختصر (slug)

يُشتقّ من العنوان تلقائيًا ويقبل العربي. بمجرّد أن تلمسه بيدك يتوقّف
الاشتقاق فلا يدوس تغييرُ العنوان على رابط اخترته.

> تغيير الـslug بعد النشر يكسر كل رابط مُشارَك للمقال. الأرشفة أفضل من
> الحذف حين تريد إخفاء مقال منشور — الأرشفة تُبقي الرابط يعمل.

### قائمة الجاهزية

مؤشّر تحريري لا حارس: يعدّ ما نقص (مقتطف، غلاف، تصنيف، وسوم، عناوين
داخلية، وصف SEO). النشر لا يُمنع إلا بمتن أقصر من 220 حرفًا؛ ما عداه تحذير
يُعرض ويُكمَّل الحفظ.

---

## ④ التصنيفات

بيانات لا ثوابت في الكود: زرّ **التصنيفات** في أعلى اللوحة. ما تضيفه يظهر
في المدوّنة فورًا بلا نشر.

- التصنيف المعطَّل يختفي من التصفّح ولا تُحذف مقالاته.
- التصنيف الفارغ (صفر مقالات) لا يُعرض للزائر — طريق مسدود مضمون.
- حذف تصنيف يترك مقالاته بلا تصنيف ولا يحذفها، واللوحة تقول لك كم مقالًا
  سيتأثّر قبل أن تؤكّد.

---

## ⑤ تحسين محرّكات البحث

- كل مقال يكتب `<title>` و`description` و`og:*` و`canonical` من بياناته.
- المقال المنشور يُصدِّر بيانات `BlogPosting` منظَّمة (Schema.org).
- المسودّة والمجدول يحملان `noindex, nofollow` ولا يُصدّران بيانات منظَّمة،
  فرابط المراجعة لا يدخل فهرس البحث قبل إقرار المقال.
- `/blog/` مضافة إلى `sitemap.xml`.

---

## ⑥ الهوية البصرية

المدوّنة تستعمل نظام تصميم لوحة الشركة نفسه: نفس التوكنز والزجاج والحواف
والمسافات من `assets/css/customer-dashboard.css`. و`assets/css/blog.css`
يضيف ما لا وجود له هناك فقط، ويعرّف لونًا خاصًّا بالمدوّنة
(`--blog-accent`) على غرار `--company-accent` — **بلا أي تغيير في
`--color-accent` العام** حتى لا تتأثّر بقيّة صفحات المنصة.

---

## ⑦ الاختبارات

```bash
npm run test:node     # يشمل blog-model + blog.render (35 + 20 تأكيدًا)
npm run test:sql      # يشمل blog.test.sql (30+ تأكيدًا على Postgres حقيقي)
```

| الملف | ما يثبته |
|---|---|
| `tests/blog-model.test.mjs` | بناء الـslug، حدود المتن المسموح، اشتقاق الحالة، قواعد النشر |
| `tests/blog.render.test.mjs` | الصفحات الحقيقية في Chromium: العرض، الحالة في الرابط، منع تنفيذ المتن، شريط المحرّر، لوحة التحرير |
| `tests/sql/blog.test.sql` | **من** يقرأ ومن يكتب — القرار الذي تتّخذه القاعدة لا المتصفح |
